{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <h1>{{ title }}</h1>

    {% for schoolYear in schoolYears %}
    <h2> ({{ schoolYear.name }}) (id: {{ schoolYear.id }})</h2>
    <p> {{ schoolYear.description }}</p>

        {% if schoolYear.startDate %}date de début: {{ schoolYear.startDate|date('d/m/Y')}}
    {% endif %}
<br>
     {% if schoolYear.endDate %}date de fin: {{ schoolYear.endDate|date('d/m/Y')}}
    {% endif %}
    
    {% endfor %} 


{% endblock %}